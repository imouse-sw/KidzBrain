<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/main"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/background_inicio"
    android:fitsSystemWindows="true"
    tools:context="com.kidzbrain.juegos.MenuJuegos">

    <FrameLayout
        android:id="@+id/btnBackContainer"
        android:layout_width="50dp"
        android:layout_height="50dp"
        android:background="@drawable/card_menu_icon"
        android:elevation="8dp"
        android:layout_margin="24dp"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent">

        <ImageView
            android:id="@+id/btnBack"
            android:layout_width="24dp"
            android:layout_height="24dp"
            android:src="@drawable/ic_back"
            android:layout_gravity="center"
            app:tint="#2D3E50" />
    </FrameLayout>

    <TextView
        android:id="@+id/tvTituloJuegos"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:fontFamily="@font/syne"
        android:text="Zona de Juegos"
        android:textColor="#FFFFFF"
        android:textSize="28sp"
        android:textStyle="bold"
        app:layout_constraintTop_toTopOf="@+id/btnBackContainer"
        app:layout_constraintBottom_toBottomOf="@+id/btnBackContainer"
        app:layout_constraintStart_toEndOf="@+id/btnBackContainer"
        app:layout_constraintEnd_toEndOf="parent"
        android:layout_marginStart="16dp" />

    <androidx.viewpager2.widget.ViewPager2
        android:id="@+id/viewPagerJuegos"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_marginVertical="40dp"
        android:paddingHorizontal="60dp"
        android:clipToPadding="false"
        android:clipChildren="false"
        app:layout_constraintTop_toBottomOf="@+id/btnBackContainer"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent" />

</androidx.constraintlayout.widget.ConstraintLayout>